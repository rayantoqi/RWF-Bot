// ═══════════════════════════════════════════════════════════════════════════════
// 📁 config.js - إعدادات البوت الرئيسية
// ═══════════════════════════════════════════════════════════════════════════════

require('dotenv').config();

module.exports = {
    // ═══════════════════════════════════════════════════════════════════════════════
    // 🤖 معلومات البوت
    // ═══════════════════════════════════════════════════════════════════════════════
    bot: {
        token: process.env.TOKEN,
        clientId: process.env.CLIENT_ID,
        clientSecret: process.env.CLIENT_SECRET,
        ownerId: process.env.OWNER_ID || 'YOUR_ID_HERE',
        prefix: process.env.PREFIX || '!',
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // 🌐 إعدادات الموقع
    // ═══════════════════════════════════════════════════════════════════════════════
    website: {
        port: process.env.PORT || 8080,
        callbackURL: process.env.CALLBACK_URL || 'http://localhost:8080/auth/discord/callback',
        sessionSecret: process.env.SESSION_SECRET || 'rayan1234_change_this_in_production',
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // 🎫 إعدادات التذاكر الافتراضية
    // ═══════════════════════════════════════════════════════════════════════════════
    tickets: {
        defaultLimit: 2,
        defaultType: 'buttons', // 'buttons' أو 'select'
        defaultColor: '#5865F2',
        categories: [
            { label: 'دعم فني', value: 'tech', emoji: '🛠️' },
            { label: 'شكوى', value: 'report', emoji: '⚠️' },
            { label: 'استفسار', value: 'ask', emoji: '❓' }
        ]
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // 💰 إعدادات الاقتصاد
    // ═══════════════════════════════════════════════════════════════════════════════
    economy: {
        dailyAmount: 500,
        dailyCooldown: 24 * 60 * 60 * 1000, // 24 ساعة
        startBalance: 1000,
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // ⭐ إعدادات المستويات
    // ═══════════════════════════════════════════════════════════════════════════════
    leveling: {
        xpPerMessage: { min: 15, max: 25 },
        xpCooldown: 60 * 1000, // 1 دقيقة
        baseXp: 100,
        xpMultiplier: 1.5,
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // 🛡️ إعدادات الحماية
    // ═══════════════════════════════════════════════════════════════════════════════
    security: {
        antiSpam: {
            enabled: true,
            messageLimit: 5,
            timeWindow: 5000, // 5 ثواني
            muteDuration: 10 * 60 * 1000, // 10 دقائق
        },
        antiLink: {
            enabled: true,
            allowedRoles: [], // IDs للرتب المسموح لها
            allowedChannels: [], // IDs للقنوات المسموحة
        }
    },

    // ═══════════════════════════════════════════════════════════════════════════════
    // 🎨 الألوان
    // ═══════════════════════════════════════════════════════════════════════════════
    colors: {
        primary: '#5865F2',
        success: '#57F287',
        warning: '#FAA61A',
        danger: '#ED4245',
        info: '#3BA55D',
        gold: '#FFD700',
    }
};
